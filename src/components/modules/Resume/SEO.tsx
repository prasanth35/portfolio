// import { Helmet } from "react-helmet";

const SEO = ({ name, role, description }:any) => {
  return (
    <></>
    // <Helmet>
    //   <title>{name} - {role}</title>
    //   <meta name="description" content={description} />
    // </Helmet>
  );
};

export default SEO